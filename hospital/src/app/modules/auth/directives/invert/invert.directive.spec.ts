import { InvertDirective } from './invert.directive';

describe('InvertDirective', () => {
  it('should create an instance', () => {
    const directive = new InvertDirective();
    expect(directive).toBeTruthy();
  });
});
