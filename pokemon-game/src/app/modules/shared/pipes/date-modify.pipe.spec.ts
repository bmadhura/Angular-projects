import { DateModifyPipe } from './date-modify.pipe';

describe('DateModifyPipe', () => {
  it('create an instance', () => {
    const pipe = new DateModifyPipe();
    expect(pipe).toBeTruthy();
  });
});
